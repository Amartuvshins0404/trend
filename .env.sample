# API Mode: "mock" | "test" | "prod"
# mock  — serves from mock/*.json (no backend needed)
# test  — proxies to API_URL_TEST
# prod  — proxies to API_URL_PROD
API_MODE=mock

API_URL_TEST=http://localhost:8001
API_URL_PROD=https://trend.byamb4.dev
